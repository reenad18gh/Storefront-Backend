{
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "watch": "ts-node-dev --respawn --transpile-only src/server.ts",
    "migrate": "db-migrate up -e dev",
    "migrate:test": "db-migrate up -e test",
    "reset": "db-migrate reset -e dev",
    "reset:test": "db-migrate reset -e test",
    "test": "ENV=test npm run migrate:test && jest"
  }
}
